
### 🎯 Interview Questions Answers - Blog Task


## 1. What are some differences between interfaces and types in TypeScript?
Ans:
``` 

TypeScript-এ interface এবং type—দুটোই object-এর structure নির্ধারণ করতে ব্যবহৃত হয়। তবে কিছু মূল পার্থক্য রয়েছে যেগুলো জানা থাকলে কোন জায়গায় কোনটা ব্যবহার করতে হবে তা সহজ হয়ে যায়।

প্রথমত, **interface declaration merging সাপোর্ট করে**, অর্থাৎ একই নামে একাধিক interface লিখলে TypeScript সেগুলোকে একত্র করে ফেলে। উদাহরণ:

```
```ts
interface A { x: string; }
interface A { y: string; }

const obj: A = { x: "xx", y: "yy" };
````

কিন্তু **type কখনো merging সাপোর্ট করে না**। অতিরিক্ত property যোগ করতে চাইলে type নতুন করে তৈরি করতে হয়।

দ্বিতীয়ত, inheritance দুটোতেই সম্ভব, তবে উপায় আলাদা। Interface-এ inheritance করা হয় `extends` ব্যবহার করে:

```ts
interface A { x: string; y: number; }
interface B extends A { z: string; }
```

অন্যদিকে type inheritance করা হয় intersection (`&`) দিয়ে:

```ts
type B = A & { j: string };
```

তৃতীয়ত, **type union (`|`) এবং intersection (`&`)—দুটোতেই সবচেয়ে flexible**। Interface দিয়ে union করা যায়, কিন্তু intersection-এর সুবিধা built-in নয়।

```ts
type Department = "dep-x" | "dep-y";
type Person = { name: string; age: number };
type Employee = { id: number; department: Department };

type EmployeeInfo = Person & Employee;
```

Interface দিয়ে union তৈরি করতে হলে type ব্যবহার করেই করতে হয়:

```ts
interface A { x: "x"; }
interface B { y: "y"; }

type C = A | B;
```

সংক্ষেপে বলা যায়—

* Interface ভালো declaration merging এবং পরিষ্কার structure-এর জন্য।
* Type ভালো জটিল টাইপ, union ও intersection তৈরিতে।

দুটোর উদ্দেশ্যই আলাদা, তাই পরিস্থিতি বুঝে যেটা উপযুক্ত, সেটাই ব্যবহার করা উচিত।


---

## 5. Provide an example of using **union** and **intersection** types in TypeScript.
Ans:


TypeScript-এ **union** এবং **intersection** টাইপ দুইটা খুবই গুরুত্বপূর্ণ এবং ব্যবহারিক ধারণা। সহজ ভাষায় বোঝা যাক।

## 🔹 Union Type (|)

Union টাইপ দিয়ে একটি ভ্যারিয়েবল একাধিক সম্ভাব্য টাইপের যেকোনো একটি হতে পারে। অর্থাৎ, value একাধিক ধরনের হতে পারে কিন্তু একসাথে নয়।  

```ts
type Status = "success" | "error" | "loading";

function showMessage(status: Status) {
    if (status === "success") {
        console.log("Operation successful!");
    } else if (status === "error") {
        console.log("Something went wrong.");
    } else {
        console.log("Loading...");
    }
}

showMessage("success");
````

উপরের উদাহরণে `status` শুধু `"success"`, `"error"` অথবা `"loading"` হতে পারে। এই বৈশিষ্ট্য union টাইপের।


## 🔹 Intersection Type (&)

Intersection টাইপ দিয়ে একটি নতুন টাইপ তৈরি করা যায়, যা একাধিক টাইপের সব property একসাথে রাখে।

```ts
type Person = {
    name: string;
    age: number;
};

type Employee = {
    id: number;
    department: string;
};

type EmployeeInfo = Person & Employee;

const emp: EmployeeInfo = {
    name: "John",
    age: 30,
    id: 101,
    department: "IT",
};
```

এখানে `EmployeeInfo` হলো `Person` এবং `Employee` এর combination। তাই `emp` object-এ উভয় টাইপের সব প্রপার্টি থাকতে হবে।

## ✔ সারসংক্ষেপ

* **Union (`|`)**: value একাধিক টাইপের যেকোনো একটি হতে পারে।
* **Intersection (`&`)**: value একাধিক টাইপের সব প্রপার্টি একসাথে রাখতে পারে।

Union এবং Intersection টাইপ ব্যবহার করে TypeScript এ আরও শক্তিশালী এবং flexible টাইপ তৈরি করা যায়।
